name: finboss
version: 0.1.0
config-version: 2

profile: finboss

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "../target"
log-path: "../logs"
packages-install-path: "../dbt_packages"

clean-targets:
  - target
  - dbt_packages

models:
  finboss:
    raw:
      +schema: raw
      +materialized: table
    std:
      +schema: std
      +materialized: table
    dm:
      +schema: dm
      +materialized: table
